- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.tmux.conf: tmux.conf
    ~/.vimrc: vimrc
    ~/.zshrc: zshrc
    ~/.config/helix/config.toml: helix.toml
    ~/.config/nvim/init.lua: neovim.lua
